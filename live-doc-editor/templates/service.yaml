apiVersion: v1
kind: Service
metadata:
  name: {{ include "live-doc-editor.fullname" . }}
  labels:
    {{- include "live-doc-editor.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "live-doc-editor.selectorLabels" . | nindent 4 }}
